name: Deploy to DreamHost

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: dreamhost-deploy
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: kim_lab_website
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: kim_lab_website/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Create DreamHost known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "${{ secrets.DREAMHOST_HOST }}" >> ~/.ssh/known_hosts

      - name: Build project
        env:
          VITE_GOOGLE_DRIVE_API_KEY: ${{ secrets.VITE_GOOGLE_DRIVE_API_KEY }}
          VITE_GALLERY_FOLDER_ID: ${{ secrets.VITE_GALLERY_FOLDER_ID }}
          VITE_LAB_FUN_FOLDER_ID: ${{ secrets.VITE_LAB_FUN_FOLDER_ID }}
          VITE_PEOPLE_FOLDER_ID: ${{ secrets.VITE_PEOPLE_FOLDER_ID }}
        run: npm run build

      - name: Deploy via rsync over SSH
        run: |
          eval "$(ssh-agent -s)"
          echo "${{ secrets.DREAMHOST_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-add ~/.ssh/deploy_key
          rsync -az --delete --chmod=D755,F644 \
            ./dist/ \
            "${{ secrets.DREAMHOST_USERNAME }}@${{ secrets.DREAMHOST_HOST }}:${{ secrets.DREAMHOST_TARGET_DIR }}"
          rm ~/.ssh/deploy_key
